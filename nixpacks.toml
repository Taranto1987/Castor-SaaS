[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.build]
cmds = ["pnpm install --no-frozen-lockfile && pnpm --filter @workspace/api-server run build"]

[start]
cmd = "node artifacts/api-server/dist/index.cjs"
